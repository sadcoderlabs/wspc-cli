# Version file generation helper 瘦身計畫

> **給 agentic workers：** 必要子技能：使用 superpowers:subagent-driven-development（建議）或 superpowers:executing-plans 逐 task 執行本計畫。步驟使用 checkbox（`- [ ]`）追蹤。

**目標：** 把 `scripts/sync-spec.ts` 與 `scripts/build-version.ts` 重複的 `SPEC_SHA` 計算、`src/version.ts` body 組裝與寫檔流程集中到一個 scripts 內部 helper。

**架構：** 新增 `scripts/version-file.ts`，只負責 sha prefix、version file body 與寫入 `<rootDir>/src/version.ts`。兩個既有 script 仍保留 fetch、package 讀取、stub decision 與 console output。

**技術：** TypeScript、Node `crypto` / `fs` / `path`、Vitest。

---

## 檔案結構

- 新增：`scripts/version-file.ts`，提供 `STUB_SPEC_SHA`、`specShaPrefix()`、`versionFileBody()`、`writeVersionFile()`。
- 修改：`scripts/sync-spec.ts`，移除本地 sha/body/write 重複邏輯，改用 helper。
- 修改：`scripts/build-version.ts`，移除本地 sha/body/write 重複邏輯，改用 helper。
- 新增：`test/version-file.test.ts`，鎖住 helper exact output 與 sha prefix。
- 修改：`test/sync-spec.test.ts`、`test/build-version.test.ts`，只在必要時收緊 version body 斷言。

## 任務 1：新增 version file helper

**檔案：**
- 新增：`scripts/version-file.ts`
- 新增：`test/version-file.test.ts`

- [ ] **步驟 1：確認 baseline**

```bash
npm test -- test/sync-spec.test.ts test/build-version.test.ts
npm run typecheck
```

預期：2 個 test files、3 個 tests 通過，typecheck 通過。

- [ ] **步驟 2：先新增 helper test**

新增 `test/version-file.test.ts`：

```ts
import { describe, expect, it } from "vitest"
import { createHash } from "node:crypto"
import {
  specShaPrefix,
  versionFileBody,
  STUB_SPEC_SHA,
} from "../scripts/version-file.js"

describe("version file helpers", () => {
  it("computes the sha256 prefix used for SPEC_SHA", () => {
    const body = "{\"openapi\":\"3.1.0\"}"
    expect(specShaPrefix(body)).toBe(
      createHash("sha256").update(body).digest("hex").slice(0, 8),
    )
  })

  it("builds the exact version file body", () => {
    expect(versionFileBody({
      generatedBy: "scripts/test.ts",
      packageVersion: "1.2.3",
      specSha: STUB_SPEC_SHA,
      fetchedAt: new Date("2026-05-26T10:00:00Z"),
      apiBase: "https://api.wspc.ai",
    })).toBe([
      "// AUTO-GENERATED by scripts/test.ts — DO NOT EDIT",
      "export const VERSION = \"1.2.3\"",
      "export const SPEC_SHA = \"00000000\"",
      "export const SPEC_FETCHED_AT = \"2026-05-26T10:00:00.000Z\"",
      "export const API_BASE = \"https://api.wspc.ai\"",
      "",
    ].join("\\n"))
  })
})
```

- [ ] **步驟 3：執行 helper test 並確認 RED**

```bash
npm test -- test/version-file.test.ts
```

預期：失敗，因為 `scripts/version-file.ts` 尚未存在。

- [ ] **步驟 4：新增 helper 實作**

新增 `scripts/version-file.ts`：

```ts
import { createHash } from "node:crypto"
import { promises as fs } from "node:fs"
import { dirname, join } from "node:path"

export const STUB_SPEC_SHA = "00000000"

export function specShaPrefix(specBody: string): string {
  return createHash("sha256").update(specBody).digest("hex").slice(0, 8)
}

export interface VersionFileArgs {
  generatedBy: string
  packageVersion: string
  specSha: string
  fetchedAt: Date
  apiBase: string
}

export function versionFileBody(args: VersionFileArgs): string {
  return [
    `// AUTO-GENERATED by ${args.generatedBy} — DO NOT EDIT`,
    `export const VERSION = ${JSON.stringify(args.packageVersion)}`,
    `export const SPEC_SHA = ${JSON.stringify(args.specSha)}`,
    `export const SPEC_FETCHED_AT = ${JSON.stringify(args.fetchedAt.toISOString())}`,
    `export const API_BASE = ${JSON.stringify(args.apiBase)}`,
    "",
  ].join("\n")
}

export async function writeVersionFile(rootDir: string, args: VersionFileArgs): Promise<void> {
  const versionPath = join(rootDir, "src/version.ts")
  await fs.mkdir(dirname(versionPath), { recursive: true })
  await fs.writeFile(versionPath, versionFileBody(args))
}
```

- [ ] **步驟 5：確認 helper test GREEN**

```bash
npm test -- test/version-file.test.ts
```

預期：通過。

## 任務 2：讓兩個 script 使用 helper

**檔案：**
- 修改：`scripts/sync-spec.ts`
- 修改：`scripts/build-version.ts`
- 修改：`test/sync-spec.test.ts`
- 修改：`test/build-version.test.ts`

- [ ] **步驟 1：更新 sync-spec**

在 `scripts/sync-spec.ts` 移除 `createHash` import，並加入：

```ts
import { specShaPrefix, writeVersionFile } from "./version-file.js"
```

把本地 sha/version body/write 區塊改成：

```ts
  const sha = specShaPrefix(body)
  const baseUrl = new URL(url).origin

  await writeVersionFile(opts.rootDir, {
    generatedBy: "scripts/sync-spec.ts",
    packageVersion: opts.packageVersion,
    specSha: sha,
    fetchedAt: now(),
    apiBase: baseUrl,
  })
```

保留 `spec/openapi.json` 寫入與 CLI console output。

- [ ] **步驟 2：更新 build-version**

在 `scripts/build-version.ts` 移除 `createHash` import 與 `STUB_SHA`，並加入：

```ts
import { specShaPrefix, STUB_SPEC_SHA, writeVersionFile } from "./version-file.js"
```

把 sha 初始值與 spec sha 計算改成：

```ts
  let sha = STUB_SPEC_SHA
```

```ts
    sha = specShaPrefix(spec)
```

把本地 version body/write 區塊改成：

```ts
  await writeVersionFile(rootDir, {
    generatedBy: "scripts/build-version.ts",
    packageVersion: pkg.version,
    specSha: sha,
    fetchedAt: now(),
    apiBase,
  })
```

保留 first-clone stub comment 與 CLI console output。

- [ ] **步驟 3：收緊既有 script tests**

在 `test/sync-spec.test.ts` 與 `test/build-version.test.ts` 中，若目前只用 `toContain()` 檢查 body，至少保留 header、`VERSION`、`SPEC_SHA`、`SPEC_FETCHED_AT`、`API_BASE` 的斷言。不要讓測試只檢查檔案存在。

- [ ] **步驟 4：focused checks**

```bash
npm test -- test/version-file.test.ts test/sync-spec.test.ts test/build-version.test.ts
npm run typecheck
git diff --check
```

預期：全部通過。

- [ ] **步驟 5：確認 generated version file 不進 git**

```bash
git status --short src/version.ts
```

預期：沒有輸出。

- [ ] **步驟 6：commit**

```bash
git add scripts/version-file.ts scripts/sync-spec.ts scripts/build-version.ts test/version-file.test.ts test/sync-spec.test.ts test/build-version.test.ts
git commit -m "refactor(scripts): share version file generation"
```

## 任務 3：PR 前檢查

**檔案：**
- 驗證全部變更檔案。

- [ ] **步驟 1：rebase 到最新 main**

```bash
git fetch origin main
git rebase origin/main
```

預期：沒有 conflict。

- [ ] **步驟 2：最終 checks**

```bash
npm test -- test/version-file.test.ts test/sync-spec.test.ts test/build-version.test.ts
npm run typecheck
git diff --check origin/main..HEAD
git status --short src/version.ts
env -u NO_COLOR FORCE_COLOR=1 npm test
```

預期：focused tests、typecheck、diff check、full suite 全部通過；`src/version.ts` 沒有被追蹤或列入變更。

- [ ] **步驟 3：Ponytail review**

檢查 diff 是否還能再縮。不要新增 class、factory、dependency 或全 repo metadata abstraction。

- [ ] **步驟 4：draft PR 與 todo comment**

```bash
git push -u origin codex/share-version-file-generation
gh pr create --draft --base main --head codex/share-version-file-generation --title "Share version file generation between scripts" --body-file /tmp/share-version-file-generation-pr.md
npx -y -p @wspc/cli@latest wspc todo comment add tod_01KVND97JEQQHP2CATRJY68QYA "<summary>"
```

預期：draft PR body 包含 Todo ID、spec path、plan path、verification commands 與 e2e-smoke note。

## 自我檢查

Spec 覆蓋：本計畫集中 sha prefix、version body、`src/version.ts` 寫入，並保留 sync-spec/build-version 各自 orchestration。

Placeholder 掃描：沒有 placeholder。

型別一致性：helper 使用 `VersionFileArgs`；兩個 script 傳入的欄位名稱與 helper 一致。
